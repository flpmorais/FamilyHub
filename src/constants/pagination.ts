/**
 * Shared page size for infinite-scroll lists:
 * - recipes, trips, suggestions: loaded 15 at a time
 * - leftovers: all active + 15 closed per page
 * - shopping: all unchecked + 15 checked per page
 */
export const PAGE_SIZE = 15;
